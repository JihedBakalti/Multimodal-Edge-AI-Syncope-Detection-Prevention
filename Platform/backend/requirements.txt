# Platform backend only (Django REST + Channels ASGI under Platform/backend/).
# From repo root:  pip install -r Platform/backend/requirements.txt

python-dotenv>=1.0.0

django>=5.0.0
djangorestframework>=3.15.0
django-cors-headers>=4.4.0

channels>=4.1.0
daphne>=4.1.0

reportlab>=4.2.0

# When PLATFORM_DB_ENGINE=postgres (see config/settings.py)
psycopg[binary]>=3.1.0
